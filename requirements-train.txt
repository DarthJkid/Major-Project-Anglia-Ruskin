# Dependencies for re-training the model (scripts/run_pipeline.py).
# The dashboard itself only needs requirements.txt.
-r requirements.txt

scikit-learn>=1.3
catboost>=1.2
lightgbm>=4.0
xgboost>=2.0
shap>=0.44
